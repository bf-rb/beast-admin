<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <title><%= @page.title || 'Beast Admin' %></title>
    <%= stylesheet_link_tag('beast_admin', media: 'all') %>
    <%= javascript_include_tag('beast_admin') %>
  </head>
  <body class="beast-admin">

    <div class="wrapper">

      <div class="header">

        <a class="btn" href="<%= BeastAdmin.routes.dashboard_path %>">
          Beast Admin
        </a>

        <%= render file: "#{@page.partials_path}/menu" %>

      </div>

      <div class="content">
        <%= render file: "#{@page.partials_path}/#{params[:action]}" %>
      </div>

    </div>

    <div class="footer">

    </div>
  </body>
</html>