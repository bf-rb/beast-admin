<% if @resources.count == 0 %>
  <h2>
    <%= @res_model_str %> is blank
  </h2>

  <a class="btn" href="<%= BeastAdmin.routes.path_helper(@res_model_str, :new) %>">
    create new <%= @res_model_str %>
  </a>

<% else %>

  <a class="btn" href="<%= BeastAdmin.routes.path_helper(@res_model_str, :new) %>">
    create new <%= @res_model_str %>
  </a>

  <table>
    <thead>
      <tr>
        <% @db_cols.each do |col| %>
          <th><%= col[:name].humanize %></th>
        <% end %>
          <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @resources.each do |res| %>
        <tr>
          <% @db_cols.each do |col| %>
            <td><%= res.send(col[:name]).to_s %></td>
          <% end %>

          <td>
            <a class="" href="<%= BeastAdmin.routes.path_helper(@res_model_str, :show, res.id) %>">
              Show
            </a>
            <a class="" href="<%= BeastAdmin.routes.path_helper(@res_model_str, :edit, res.id) %>">
              Edit
            </a>
            <a class="" data-confirm="Are you sure?" data-method="delete" href="<%= BeastAdmin.routes.path_helper(@res_model_str, :delete, res.id) %>" rel="nofollow">
              Delete
            </a>
          </td>

        </tr>

      <% end %>
    </tbody>


  </table>



<% end %>